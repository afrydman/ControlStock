namespace BusinessComponents
{
    public static class personal
    {
        //public static List<personalData> getPersonalbyLocal()
        //{
        //    return personalDataMapper.getPersonalbyLocal(helper.IDLocal);
        //}

        //public static List<personalData> getAll(bool connLocal = true)
        //{

        //    return personalDataMapper.getAll(connLocal);
        //}


        //internal static personalData getPersonalbyId(Guid idPersonal)
        //{
        //    return personalDataMapper.getPersonalbyId(idPersonal);
        //}

        //public static bool insert(personalData p, bool connLocal = true)
        //{
        //    if (p.ID == new Guid())
        //    {
        //        p.ID = Guid.NewGuid();
        //    }
        //    return personalDataMapper.insert(p, connLocal);
        //}

        //public static bool update(personalData p, bool connLocal = true)
        //{
        //    return personalDataMapper.update(p, connLocal);
        //}
    }
}
