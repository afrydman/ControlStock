namespace BusinessComponents
{
    public class tipoIngreso
    {

        //public static List<tipoRetiroData> getAll()
        //{
        //    return getAll(false);
        //}
        //public static List<tipoRetiroData> getAll(bool soloRetiroManual,bool connLocal = true)
        //{

        //    List<tipoRetiroData> a = ingresoTipoDataMapper.getAll(connLocal);
        //    if (soloRetiroManual)
        //    {
        //        a = a.FindAll(delegate(tipoRetiroData aux) { return aux.ID != helper.IDrecibo && aux.ID != helper.IDextraccioCuenta; });
        //    }
        //    return a;
        //}

        //internal static tipoRetiroData getbyId(Guid id)
        //{
        //    return ingresoTipoDataMapper.getbyID(id);
        //}

        ////public static bool insert(tipoRetiroData p, bool connLocal = true)
        ////{
        ////    if (p.ID==new Guid())
        ////    {
        ////        p.ID = Guid.NewGuid();
        ////    }
        ////    return ingresoTipoDataMapper.insert(p, connLocal);
        ////}

        //public static bool update(tipoRetiroData p, bool connLocal = true)
        //{
        //    return ingresoTipoDataMapper.update(p, connLocal);
        //}
    }
}
