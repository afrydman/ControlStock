namespace BusinessComponents
{
    public class tipoRetiro
    {

        //public static List<tipoRetiroData> getAll()
        //{
        //    return getAll(false);
        //}
        //public static List<tipoRetiroData> getAll(bool soloRetiroManual, bool connLocal = true) {

        //    List<tipoRetiroData> a = retiroTipoDataMapper.getAll(connLocal);
        //    if (soloRetiroManual)
        //    {
        //        a = a.FindAll(delegate(tipoRetiroData aux) { return aux.ID != helper.IDordenpago && aux.ID != helper.IDdepositoCuenta; });
        //    }
        //    return a;
        //}

        //internal static tipoRetiroData getbyId(Guid id)
        //{
        //    return retiroTipoDataMapper.getbyID(id);
        //}

        //public static bool insert(tipoRetiroData p, bool connLocal = true)
        //{
        //    if (p.ID==new Guid())
        //    {
        //        p.ID = Guid.NewGuid();
        //    }
        //    return retiroTipoDataMapper.insert(p,connLocal);
        //}

        //public static bool update(tipoRetiroData p, bool connLocal = true)
        //{
        //    return retiroTipoDataMapper.update(p,connLocal);
        //}
    }
}
